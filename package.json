{
  "name": "calgen",
  "version": "1.0.0",
  "description": "智能日程文本解析与日历生成工具",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npx cap sync",
    "cap:build": "npm run build && npx cap sync",
    "postinstall": "node scripts/patch-plugin.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capgo/capacitor-calendar": "^8.0.9"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "vite": "^6.2.0"
  }
}
